package com.gogulf.passenger.app.utils.enums


enum class DefaultInputType {
    Text,
    Number,
    Email,
    FullName,
    CapWord,
    CapSentence,
    PassNumber,
    Password
}

enum class HistoryType {
    Completed,
    Cancelled,
    Pending,
    Confirmed
}