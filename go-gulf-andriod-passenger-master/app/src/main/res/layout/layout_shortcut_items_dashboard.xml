<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@id/mainContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/m_5dp"
        android:layout_marginBottom="@dimen/m_5dp"
        android:padding="@dimen/m_5dp">

        <LinearLayout
            android:id="@+id/shortcutImageLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/shortcutImage"
                android:layout_width="@dimen/m_24dp"
                android:layout_height="@dimen/m_24dp"
                android:layout_margin="@dimen/m_8dp"
                android:layout_marginTop="@dimen/m_5dp"
                android:contentDescription="@string/si"
                app:tint="@color/primaryColor"
                android:src="@drawable/ic_delete_home" />
        </LinearLayout>


        <TextView
            android:id="@+id/shortcutName"
            style="@style/textStyle12"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/m_5dp"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="@color/black"
            android:text="Home"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/shortcutImageLayout" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>